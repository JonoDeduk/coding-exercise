const pathToResultsPage = '/results/:id';
const pathToSearchPage = '/';

const linkToResultsPage = (id) => `results/${id}`;

export const paths = {
  pathToResultsPage,
  pathToSearchPage,
  linkToResultsPage
};

export default paths;
